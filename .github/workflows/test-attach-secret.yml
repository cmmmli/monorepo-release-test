name: release-main

on:
  push:
    branches:
      - main
      - test-attach-secret
jobs:
  test-attach-secret:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      # - uses: actions/checkout@v4
      #   with:
      #     ref: main
      - name: echo dummy secret
        env:
          TEST_USER: ${{ vars.TEST_USER }}
          DUMMY_SECRET2: ${{ secrets.DUMMY_SECRET2 }}
        run: |
          echo "https://${TEST_USER}:${DUMMY_SECRET2}example.net"

